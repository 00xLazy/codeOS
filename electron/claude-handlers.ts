import type { IpcMain, BrowserWindow } from 'electron'

export function setupClaudeHandlers(_ipcMain: IpcMain, _win: BrowserWindow | null) {
  // Claude Agent SDK streaming handlers
  // TODO: integrate @anthropic-ai/claude-agent-sdk
}
